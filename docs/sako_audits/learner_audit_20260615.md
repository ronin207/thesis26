# Learner comprehension audit — 2026-06-15 (3 cold readers + Sako synthesis)

Lens: a new lab member who knows general crypto but NOT this project, reading the deck cold.

## Verdict: the thread does NOT hold for a newcomer (all 3 readers could only retell by reverse-engineering)

### Top 3 causes of the 'messy' feeling
- Background arrives after the slides that depend on it, and the real intro lives in the backups. Slides 5-7 (Problem, Challenges, Field-Mismatch) lean on BDEC/PLUM/Aurora/zkVM/substrate as if defined, but the BDEC background is slides 8-9 and the foundational zkVM-vs-compiler contrast (20), field-emulation tax (21), and clearest pipeline diagram (23) are all in BACKUP after the conclusions. Every reader spent the early slides holding a bag of undefined names. This is an ordering problem, not a content problem.
- Undefined vocabulary used as if known. The entire load-bearing lexicon — BDEC, PLUM, Griffin, zkVM, precompile, substrate, circuit, R1CS, AIR, STARK, Aurora, non-native field, limb arithmetic, EUF-CMA, T2, DNF, OOM, lambda — is deployed before (or without ever) being defined. The two terms that carry the central causal story, 'non-native field' and 'limb arithmetic' (slide 13), are never defined at all.
- A few slides are visually overloaded or broken. Slide 7 jams text into the top third (looks like a layout bug) while dumping seven new terms; slides 12, 13, 16, 17 cram pipelines/tables/lab-notes past the point of parsing. Compounded by the headline negative result being buried in tiny text on slide 12 yet repeated three times (12/15/16), and by inconsistent naming (agenda's 'Trilemma/Inversion/Wall' never reappear as titles; precompile is 'RISC Zero/SP1' on slide 10 but 'SP1' on slide 11).

### Thread assessment
The thread does NOT hold for a true newcomer on first pass — all three readers could only retell the thesis by reverse-engineering it, and each named specific things they could not state confidently afterward (what PLUM is, what 'the Inversion'/'T2' are, whether the contribution is BUILD or MEASURE, and whether the security claims are proven or conjectured). The spine itself is sound and genuinely interesting (BDEC anonymous PQ credentials -> the flexible/PQ/private trilemma -> two substrates measured -> the inversion mechanism -> each buys two never three -> open soundness gaps), but it is delivered punchline-first with definitions trailing or banished to backups, so the audience assembles meaning instead of receiving it. The single highest-leverage change is to fix the ordering: move the BDEC background (8-9) and the foundational backups (20 zkVM-vs-compiler, 21 field-emulation tax, 23 clean pipeline) UP to before the Problem Statement, so the talk defines its terms and shows its core problem BEFORE the slides that depend on them. That one reorder dissolves the largest cluster of confusions (the 'bag of undefined names' across 5-7) without rewriting content, and it lets the clear backup figures replace the messy main-line figures (12/16) as a free side effect.

### Highest-leverage change
Reorder for definition-before-use: pull the BDEC background (slides 8-9) and the three foundational backup slides — zkVM-vs-SNARK-compiler (20), the field-emulation tax (21), and the clean proving-pipeline diagram (23) — UP ahead of the Problem Statement (5), and let the clear backup figures replace the messy main-line figures on 12 and 16. This single structural move (no new content required) dissolves the most-cited confusion cluster — the 'bag of undefined names' carried across slides 5-7 — because BDEC, PLUM, zkVM-vs-circuit, substrate, and the field mismatch all get defined before any slide leans on them, and the audience receives the spine instead of reverse-engineering it.

## Punch list (51 items)

### HIGH
- **3 A credential issued in 2026** — First content slide drops FIPS 204/205, eIDAS Art. 5a(4), IETF CFRG, BBS, DLs, ZK undefined. 'BBS not post-quantum' asserted with no idea what BBS is; 'DLs' ambiguous.
  - FIX: Define on first use: BBS = the standard unlinkable credential signature (and why it's broken by quantum: discrete-log based); spell 'DLs' = driver's licenses; one-line gloss for eIDAS (EU digital-identity regulation). Cut FIPS/CFRG citations to a footnote.
- **3 A credential issued in 2026** — Three bullets read as mandates/observations, then green punchline says 'a deployer needs all three at once' — reader must retroactively re-bin the bullets into three named properties.
  - FIX: Front-label each bullet with its property name (Post-quantum / Unlinkable / Runs-on-device) so 'all three' maps 1:1 to the three bullets without re-reading.
- **4 Can a proof be flexible, post-quantum, private?** — 'circuit' and 'rebuild' appear for the first time inside the definition of 'Flexible' but are themselves undefined — load-bearing for the whole thesis.
  - FIX: Add a half-line gloss on first use: 'circuit = the fixed program a SNARK is compiled into; changing the policy forces recompiling and re-auditing it.' This is the concept the whole talk turns on; pay for it here.
- **4 Can a proof be flexible, post-quantum, private?** — Slide 3's three requirements (post-quantum/unlinkable/on-device) are silently relabeled into slide 4's three circles (Post-Quantum/Private/Flexible) plus a dangling fourth 'on a personal computer?' — reader sees four constraints and no mapping.
  - FIX: Draw the mapping explicitly: 'unlinkable = Private', 'on-device = the personal-computer constraint applied to all three'. Make 'on a personal computer' the frame/hardware condition of the whole Venn, not a fourth circle. Add one bridge line tying slide 3's three to these three.
- **5 Problem Statement** — BDEC, Aurora, PLUM, zkVM, guest-program all appear for the first time as if known; the Background that explains BDEC is slides 8-9, AFTER this slide. Readers held a 'bag of undefined names' for slides 5-7.
  - FIX: REORDER: move the BDEC background (8-9) before the Problem Statement (5), or add a 3-line 'cast of terms' box at the top of slide 5 (BDEC = anonymous post-quantum credential system; PLUM = its post-quantum signature; zkVM = general-purpose proving machine you write a program for; Aurora = a fixed-circuit SNARK). This is the single most-cited ordering complaint.
- **5 Problem Statement** — Three competing phrasings of THE research question stack up (run-on-own-machine? cost-of-change? is-a-zkVM-viable?); newcomer can't tell which is primary.
  - FIX: Pick ONE primary question in bold ('Is a general-purpose zkVM a viable substrate for BDEC-PLUM on a personal PC?') and demote the other two to sub-questions under it.
- **6 The Challenges** — Comparison table mixes a property column 'General-purpose zkVM' (a mechanism) with property columns (Post-quantum, ZK, Consumer hardware). And 'This thesis (BDEC-PLUM)' has an X under Zero-knowledge, seemingly contradicting slide 4's Private goal.
  - FIX: Either rename the zkVM column to a property ('flexible / general-purpose') or visually separate the 'approach' column from the 'achieves' columns. Add a footnote on the ZK X: 'baseline STARK is not ZK by default; ZK is the open gap, see slide 16' so it doesn't read as failing its own goal.
- **7 The Field-Mismatch Cost** — LAYOUT: text crammed into top third, bottom two-thirds blank — reads like a rendering bug. Most-cited 'messiest' slide (reader 1).
  - FIX: Re-balance the layout: enlarge the text or add a simple diagram (192-bit operand decomposed into ~7 BabyBear limbs) to fill the empty space. Visual error undermines the slide before content is read.
- **7 The Field-Mismatch Cost** — One bullet introduces precompiles, algebraic-hash, Griffin, '192-bit prime field', BabyBear, '31-bit architecture', RISC-V cycles — none defined. And 'field-mismatch' (the title) is never plainly defined as 'big field emulated inside small field'.
  - FIX: Lead with the one-sentence definition: 'Field mismatch = PLUM's large 192-bit field must be simulated on top of the zkVM's small 31-bit BabyBear field.' Cut or footnote the term cascade; define Griffin (algebraic hash) and precompile (accelerated subroutine) on first use; split into two slides if needed.
- **7 The Field-Mismatch Cost** — States the 2-3k cycles/mul cost but never says its role: is the thesis BUILDING precompiles to fix it or just MEASURING it? Readers left unsure whether the contribution is a fix or a measurement.
  - FIX: Add one line stating the role: 'this thesis measures this cost and builds a Griffin precompile to attack it (slide 10).' Resolve the build-vs-measure ambiguity flagged by two readers.
- **8 Background (BDEC diagram)** — REORDER/late: first slide that actually defines BDEC (Issuer/Bob/Verifier/Ledger) but arrives AFTER slides 5-7 already used BDEC/PLUM/zkVM as known. Cited by all readers in range.
  - FIX: Move slides 8-9 before slide 5 (or before slide 3's deep dive). Background must precede the Problem Statement that depends on it.
- **8 Background (BDEC diagram)** — Arrow labels CreGen, CreVer, ShowCre, ShowVer, RevCre, ConditionalLink given with zero definitions; 'BDEC' only in the citation footnote, never explained on-slide.
  - FIX: Add a compact legend defining each protocol step in 3-5 words (CreGen = credential generation, ShowCre = show/prove a credential, RevCre = revoke, ConditionalLink = conditional de-anonymization). State 'BDEC = this whole anonymous-credential protocol' on the slide body.
- **9 Background (k:1 to 2)** — C_static(w), C_dynamic(w), w, and 'KeyGen over a circuit C' are unexplained; applying KeyGen TO a circuit is unusual notation a newcomer won't recognize, yet the inequality is the bolded punchline.
  - FIX: Define in one line: 'C = the predicate being proven (a boolean circuit), w = the witness/secret inputs; KeyGen(C) = generating the SNARK keys FOR that specific circuit.' Then the inequality reads naturally.
- **9 Background (k:1 to 2)** — Chain k:1->2 => C_static changes => KeyGen differs => re-run Setup is compressed into a therefore-symbol; the crux of the 'flexibility' motivation is not walked through. 'Setup' capitalized but never defined (trusted setup? signature keygen?).
  - FIX: Add a 3-step arrow chain in words: 'add a 2nd credential -> the predicate (circuit) changes -> its keys differ -> the whole SNARK Setup must be redone.' Define 'Setup = the SNARK's (per-circuit) key generation' to disambiguate from signature keygen.
- **10 Contributions** — Griffin, Fp192, zkVM, precompile, 'prove-mode host', 'substrate', RISC Zero, SP1, Aurora, lambda all dropped undefined; 'substrate' is the organizing concept of the talk and gets its first use here without a gloss.
  - FIX: Define 'substrate = the underlying proving system you run the predicate on (a zkVM, or a static SNARK).' Gloss Griffin (algebraic hash), Fp192 (the 192-bit field), precompile (accelerated subroutine). If background is reordered earlier, most of these are already covered.
- **11 Contributions** — INCONSISTENT: slide 10 says precompile is on 'RISC Zero / SP1'; contribution 1 here says 'on SP1' only. Reader can't tell if the artifact is SP1-only or both.
  - FIX: Make the two slides agree. State plainly which substrate the precompile is built on (and if measured on both, say so) consistently across slides 10/11/16.
- **12 Measured (pipeline + 4 captions)** — TOO DENSE: ~8 near-invisible grey boxes overlaid as a pipeline, plus four color-coded caption boxes each firing numbers; reader can't tell what the central figure depicts or how captions map to it. Co-messiest slide (reader 2).
  - FIX: Split into two slides or drastically simplify: one clean pipeline figure with readable boxes, captions placed adjacent to the stage they annotate. Use the clear backup-23 pipeline as the main version (see reorder note).
- **12 Measured (pipeline + 4 captions)** — Undefined fly-by terms: DNF, 'SHA-3 control', n=2/n=3, 'zk-disabled', 'sign robust'. Reader can't decode DNF or why Aurora is measured with ZK disabled.
  - FIX: Define on first use: DNF = did-not-finish (within budget); 'SHA-3 control' = the SHA-variant baseline (Cell 3); n = number of repeated runs; 'zk-disabled' = ZK turned off for measurement, with a note on why. Add a tiny glossary footer.
- **12 Measured (pipeline + 4 captions)** — The key negative result ('ZK wrap OOMs at ~20/24 GB; no PQ-ZK proof from either zkVM') is buried as the 4th caption in tiny pink text; readers skim past the headline finding.
  - FIX: Promote the negative result to a single prominent banner ('Headline: no post-quantum + zero-knowledge proof reachable on either substrate — ZK wrap OOMs') and state it ONCE prominently rather than three times faintly.
- **13 The Inversion** — TOO DENSE: 3x2 table crams per-cell numbers + CHEAP/SLOW marks; hard to parse which number belongs to which substrate. '(16,12)' and superscripts [7],[7,12] unexplained. Conceptual heart of the thesis delivered as a cramped table.
  - FIX: Restructure as a clear two-column 'Aurora vs zkVM' comparison with the ranking flip visualized (Griffin: cheap->slow; SHA: slow->cheap). Define '(16,12)' (parameters) and the bracket superscripts (citation refs) or drop them from the headline cell.
- **13 The Inversion** — Mechanism of the inversion ('cost ~ #R1CS constraints' in static circuit vs 'cost = committed trace area' in zkVM) is in tiny right-column text; the leap from 'R1CS-cheap' to '6.6M cycles' is not bridged. This is the causal core of the thesis.
  - FIX: Give the mechanism its own line, plainly: 'In a fixed circuit, cost = number of algebraic constraints, so Griffin (algebraic) is cheap. In a zkVM, cost = CPU cycles, and Griffin's 192-bit math must be done in multi-limb software -> 6.6M cycles. SHA fits the small native ops, so it flips to cheap.'
- **13 The Inversion** — 'committed trace area', 'chip area', 'non-native field', 'limb arithmetic' — the load-bearing explanatory terms — are all undefined jargon.
  - FIX: Define inline: 'non-native field = a field bigger than the proving system's native one; limb arithmetic = simulating big-field ops with several small-field words.' These two terms carry the whole causal story; do not leave them undefined.
- **16 Where the Wall Sits** — TOO DENSE + REDUNDANT: dense left prose AND a right two-column flowchart say much the same thing; same negative conclusion now stated a 3rd time (after 12 and 15) in yet another visual form. Co-messiest (reader 3).
  - FIX: Cut the duplication: keep ONE representation (prefer the clear backup-23 flowchart) and delete the redundant prose, or make the prose add genuinely new content. State the negative result once, here, definitively.
- **16 Where the Wall Sits** — Framing silently shifts from slide-15's clean 'pick two of three' trade-off to concrete engineering walls (OOM-killed, vendor-disclaimed, x86-only) — reader can't tell if the missing third is a fundamental limit or a 24 GB-laptop artifact. Also OOM, lift/join, Groth16 x86-only undefined; box colors have no legend.
  - FIX: Add one bridge sentence classifying each wall: 'OOM = artifact of 24 GB (may lift on bigger RAM); vendor-disclaim = fundamental to the default STARK; x86-only = tooling artifact of Apple Silicon.' Define OOM = out-of-memory on first use; add a color legend to the flowchart.
- **17 Open Items** — TOO DENSE: six bullets each stacking 3-5 undefined terms (T2, Griffin AIR soundness, non-MDS circulant, 'L3 reads the updated lane', FGLM model, FreeLunch CRYPTO 2024, Poseidon2, composite p0, lookup-borne families). Reads like raw lab notes; no hierarchy. Co-messiest (reader 3).
  - FIX: Group into 2-3 themed clusters with a header each (e.g. 'Soundness gaps', 'Cryptanalysis', 'Measurement noise'); cut or footnote the deepest jargon; keep at most one technical term per bullet. This is an internal-notes slide that needs an audience-facing pass.
- **17 Open Items** — The Griffin '14-round count sized against the superseded FGLM model, 128-bit margin unvalidated (not broken)' is listed as one of six equal items; reader can't gauge if it threatens the whole thesis or is a footnote. Three readers flagged severity ambiguity.
  - FIX: Rank the open items by severity and label this one explicitly: 'Most consequential / least consequential.' State in plain words: 'unvalidated = needs re-checking against the newer model; not a known break.' Make the security margin's status unambiguous.
- **20-21-23 Backups (zkVM-vs-compiler, field-emulation tax, proving pipeline)** — The genuinely foundational material — what a zkVM vs SNARK-compiler is (20), the field-emulation tax that is the core problem (21), and the clearest proving-pipeline diagram (23) — all live in BACKUP, AFTER the conclusions. Two readers independently said the real intro is in the appendix, which is backwards.
  - FIX: PROMOTE these three to the main line: slide 20's zkVM-vs-compiler contrast belongs near slide 4-5; slide 21's field-emulation tax belongs at slide 7 (replace/merge the messy slide 7); slide 23's clean pipeline should REPLACE the dense slide 12/16 figure. Highest-structural-leverage reorder.

### MED
- **2 Agenda** — 'Trilemma', 'the Inversion', 'the Wall' are named in the agenda as if known; none is the literal title of any later slide, so the scanner is promised concepts that never reappear under those names.
  - FIX: Rename agenda items to match later slide titles, or add a one-line gloss in parentheses: 'the Trilemma (Flexible/Post-Quantum/Private, slide 4)', 'the Inversion (slide 13)', 'the Wall (slide 16)'. Make every agenda noun resolve to a body slide.
- **3 A credential issued in 2026** — '(>=3 GB RAM; $100k/10 kW)' dropped with no referent — reader cannot tell what the numbers measure or why they show client-side ZK is hard.
  - FIX: Label the comparison explicitly: 'client-side ZK today needs server-class resources (>=3 GB RAM, ~$100k / 10 kW hardware) — out of reach for a phone.' State what each number is the cost OF.
- **4 Can a proof be flexible, post-quantum, private?** — Venn asks 'can a proof be all three?' but never marks whether the center is achievable or empty; the answer ('buys two, never three') only lands on slide 15.
  - FIX: Add a question mark or '?' badge in the center region and one line: 'is the center reachable? — answered on slide 15.' Foreshadow that the center is the contested region.
- **5 Problem Statement** — Core claim — a guest-program edit in a zkVM is cheaper than re-deriving an Aurora circuit — is asserted in one line before being motivated. That comparison IS the thesis.
  - FIX: Add one bridge sentence or defer the claim: 'why a guest edit beats re-deriving a circuit is shown on slides 9 (circuit rebuild cost) and 13 (the inversion).' Don't state the punchline before its support.
- **6 The Challenges** — Argo et al. (CCS 2024), Policharla et al. (2023), CAPSS (2025) cited by ia.cr ID with no one-line description; reader can't judge the comparison.
  - FIX: Add a 3-5 word descriptor after each: e.g. 'Argo — post-quantum anon-cred in a SNARK', 'CAPSS — post-quantum credential proving system'. Without summaries the checkmark pattern is unevaluable.
- **8 Background (BDEC diagram)** — Gray vs black coloring of diagram elements is unexplained — reader can't tell if it's meaningful (out-of-scope vs in-scope) or styling.
  - FIX: Add a one-line legend: 'grey = part of BDEC but out of this thesis's scope; black = what we measure.' Or unify the styling if the distinction isn't intended.
- **8 Background (BDEC diagram)** — Two JSON-like blocks (Q Tech {Job,Level,Duration}, XYZ University {Name,Degree,Year,GPA}) float unlabeled; reader must guess they are Bob's credential contents.
  - FIX: Label them: 'Bob's credentials — one from an employer (Q Tech), one from a university (XYZ).' Draw which issuer issues which.
- **9 Background (k:1 to 2)** — This is the concrete instance of the slide-4 'Flexible' circle but never says so; the payoff line back to the Venn is left implicit.
  - FIX: Add a callout: 'This is the Flexibility problem from slide 4, made concrete.' Draw the line from 'must re-run Setup' to the green circle.
- **10 Contributions** — 'lambda=80 (largest level observable in budget)' — lambda not flagged as the security parameter; 'observable in budget' cryptic.
  - FIX: Write it out: 'lambda = security parameter; lambda=80 (80-bit) is the largest level that fits the 24 GB / time budget — higher lambda runs out of memory.'
- **11 Contributions** — 'update churn', 're-audit footprint', 'at parity' undefined; contribution 3's 'per-substrate obstruction to PQ-ZK' announced as a finding without saying what the obstruction IS (teaser, not contribution).
  - FIX: Define update churn (work caused by a scheme/parameter change) and re-audit footprint (how much must be re-proven/re-checked). For contribution 3, name the obstruction in one clause: 'the ZK-wrap step OOMs / is vendor-disclaimed (slide 16).'
- **14 Security theorem + game** — Informal theorem packs EUF-CMA, PLUM, e_KS, e_AIR, 'lookup binding', 'AIR soundness', T1/T2/T3 — five+ undefined symbols in one line; PLUM essentially introduced here for reader 2.
  - FIX: Define each: EUF-CMA = standard unforgeability notion; e_KS, e_AIR = the (small) soundness errors of the key-switch and the AIR; T1/T2/T3 = the three assumptions, each in one clause. Define PLUM earlier (background reorder).
- **14 Security theorem + game** — Right-side adversary/challenger diagram is a standard unforgeability game but nothing labels it as such; pp and Q_issued undefined.
  - FIX: Title the diagram 'Unforgeability game (supports the theorem at left)'; gloss pp = public parameters, Q_issued = set of issued credentials.
- **14 Security theorem + game** — Theorem is stated then half-retracted (T2 'per-layer proven, lookup layer open'; anonymity needs e_ZK 'no measured pipeline supplies one'). Reader can't tell what is proven vs assumed.
  - FIX: Add an explicit status line: 'Unforgeability: proven conditional on T2 (lookup layer still open). Anonymity: NOT achieved in the measured pipeline (no ZK simulator).' Separate proven from open visually.
- **15 Each substrate buys two, never three** — Numbers (32.5 min, 22 min, recompile ~0.3s, PLONK OOM ~20/24GB) asserted with no link back to slide 12's measurements and no baseline for whether 32.5 min is good or catastrophic.
  - FIX: Add 'measured on slide 12' provenance and one baseline anchor ('vs Aurora 3.76 min / vs a target of X'). Gloss 'recompile ~0.3s, cost is assurance' = 'cheap to recompile, but you pay in re-audit assurance.'
- **15 Each substrate buys two, never three** — 'post-quantum*' asterisk has no visible footnote; 'NOT Zero-Knowledge (Succinct STARK)' assumes reader knows a plain STARK isn't ZK by default. Both flagged by two readers.
  - FIX: Add the missing footnote for the asterisk; add a half-line 'a succinct STARK proves correctness but is not zero-knowledge unless wrapped (Groth16/PLONK).'
- **16 Where the Wall Sits** — RISC Zero 6h19m vs SP1 ~32min presented without saying if it's the same workload; 'blinded' appears as a property with no explanation.
  - FIX: State whether the two numbers are the same workload (and if not, what differs); gloss 'blinded' (the RISC Zero ZK-blinding option) in one clause.
- **17 Open Items** — 'Wall-clock variance: thermal alone moves identical runs 1.44x' quietly casts doubt on every headline timing (32.5/22 min) without saying how it's handled.
  - FIX: Add one line tying it to the headline numbers: 'reported times carry n run counts and the 1.44x thermal band; the cross-substrate gaps (12-17x) exceed this noise.' Reassure that the comparisons survive the noise.
- **21 Field-Emulation Tax (backup)** — 56x cycle win appears only in a backup caption; relationship to slide-15 wall-clock (32.5 min) unclear; 'wall-clock back-off' ominous and undefined.
  - FIX: When promoted, state plainly: 'the precompile cuts guest cycles 56x, but wall-clock falls less ('back-off') because non-cycle stages (commit/FRI) now dominate.' Define 'wall-clock back-off' = the wall-time gain is smaller than the cycle gain.
- **22 Per-Row Soundness Game (backup)** — Formal T2 game shown but T2 itself is never plainly defined anywhere (referenced on 14, 17, 22 as if known); AIR/STARK trace layout (rows=steps, columns=registers) assumed.
  - FIX: State T2 in one plain sentence wherever it first appears: 'T2 = the Griffin AIR is sound (its constraints actually force correct hashing).' Add a one-line 'AIR = the table of constraints a STARK proves; rows = computation steps, columns = registers.'

### LOW
- **15 Each substrate buys two, never three** — Leader line from center to 'on a 24GB MacBook Pro' is ambiguous (whole diagram or just center?); unclear which two-circle overlap each text block sits in.
  - FIX: Place the hardware caption as a frame label for the whole Venn ('all results on a 24 GB MacBook Pro'); position each substrate block clearly inside its two-circle lens, or add small connector dots.
- **18 Plan** — Loquat appears for the first time in reader-3's range with no definition vs PLUM; parameter tuple '(14, d=3, t=4, ~2^199)' given without saying what each symbol is.
  - FIX: Gloss 'Loquat = PLUM's predecessor signature scheme (same BDEC structure)'; expand the tuple ('14 rounds, degree d=3, state t=4, ~2^199 field'). Define on first use even on a plan slide.
- **18 Plan** — Dated PM plan lands without the reader having the one-sentence core claim anchored.
  - FIX: Add a one-line claim restatement at the top of the plan slide so the to-do list has an anchor: 'Claim: each substrate buys two of {flexible, PQ, private}; remaining work closes the soundness + measurement gaps.'
- **19 References** — Rendered too small/blurry to read individual entries.
  - FIX: Increase font size / split across two slides so citations are legible. Pure legibility fix.
- **24-25 CreGen code (Loquat vs Plum)** — Two near-identical code slides; the point (PLUM is a drop-in replacement for Loquat in the same BDEC structure) is never stated; both carry the same '// guest: bdec_cregen' comment which confuses which is which; h_ta, ppk, cred, psk undefined and the AND of two verifies unexplained.
  - FIX: Add a title stating the point: 'Same BDEC CreGen, swap Loquat -> PLUM (only the verify type changes).' Differentiate the comments (Loquat vs PLUM); gloss the args (cred = credential, psk = proving secret key, ppk = ..., h_ta = ...) and note why two signatures are AND-ed (issuer-sig AND holder-binding).

## Applied 2026-06-15 (order-independent quick wins)
- S6: ZK-misread note ('✗ ZK = baseline STARK not ZK by default, the open wall, not a failure') + competitor descriptors (lattice anon-cred / PQ anon-cred on a STARK / SNARK-friendly PQ sigs).
- S3: glossed BBS (the standard unlinkable credential signature), spelled out 'driver's licenses', clarified $100k/10kW as 'server-class resources'.
- S7: prepended a plain field-mismatch definition + the MEASURE+BUILD role line.

## NOT yet applied — needs Operator go (structural / order-dependent)
- **THE REORDER (highest leverage):** move Background (S8-9) + the foundational backups (zkVM-vs-compiler, field-emulation tax, clean pipeline) UP before the Problem Statement; let the clean backup figures replace the dense main-line figures (S12, S16).
- De-densify S12/S13/S16/S17; promote the negative-result banner on S12; group S17 open items by theme + rank severity.
- Define-on-first-use glosses + 'see slide N' references (do AFTER the reorder so numbers are stable).
