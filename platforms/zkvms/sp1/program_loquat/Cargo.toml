[package]
name = "loquat_verify_program"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "loquat_verify"
path = "src/main.rs"

[dependencies]
sp1-zkvm = "6.0.1"
serde = { version = "1.0", default-features = false, features = ["alloc", "derive"] }
bincode = "1.3"
# Phase B6.4 — wires through vc-pqc's guest-only Loquat verify path
# in a successive commit.
